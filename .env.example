# Optional future HTTP AI provider. Do not put real keys in git.
# Roblox scripts cannot read this file at runtime — copy values into
# src/server/Config.luau locally (Ai.ApiKey / Ai.Endpoint) and never commit them.
# Also enable Studio: Home → Game Settings → Security → Allow HTTP Requests.

MEO_AI_PROVIDER=mock
MEO_AI_ENDPOINT=
MEO_AI_API_KEY=
MEO_AI_MODEL=gpt-4o-mini
